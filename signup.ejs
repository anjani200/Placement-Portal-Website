<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Signup Form</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #0f172a;
      color: #ffffff;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .loginbox {
      width: 300px;
      padding: 40px;
      background-color: #1e293b;
      box-shadow: 0 2px 15px rgba(56, 189, 248, 0.2);
      border-radius: 8px;
      text-align: center;
    }

    .loginbox h1 {
      font-size: 28px;
      margin-bottom: 20px;
      color: #38bdf8;
    }

    .loginbox p {
      font-size: 16px;
      color: #cbd5e1;
      margin-bottom: 5px;
      text-align: left;
    }

    .loginbox input[type="text"],
    .loginbox input[type="password"],
    .loginbox select {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      background-color: #1e293b;
      color: #ffffff;
      border: 1px solid #7dd3fc;
      border-radius: 4px;
      font-size: 16px;
      box-sizing: border-box;
    }

    .loginbox input[type="submit"] {
      width: 100%;
      padding: 12px;
      background-color: #38bdf8;
      border: none;
      color: #ffffff;
      font-size: 18px;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      margin-top: 20px;
    }

    .loginbox input[type="submit"]:hover {
      background-color: #32b7f4;
    }

    .loginbox a {
      display: block;
      color: #94a3b8;
      font-size: 14px;
      text-decoration: none;
      margin-top: 10px;
      transition: color 0.3s ease;
    }

    .loginbox a:hover {
      color: #38bdf8;
    }

    .avataar {
      width: 120px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="loginbox">
    <img src="https://upload.wikimedia.org/wikipedia/commons/7/75/IIIT-BH_Official_Logo.png" class="avataar">
    <h1>Signup</h1>
    <form action="/signup" method="POST">
      <p>Username</p>
      <input type="text" name="username" placeholder="Enter Username" required>
      <p>Password</p>
      <input type="password" name="password" placeholder="Enter Password" required>
      <p>Select</p>
      <select name="isStudent" id="isStudent" required>
        <option value="true">Student</option>
        <option value="false">Company</option>
      </select>
      <input type="submit" value="Sign Up">
    </form>
  </div>
</body>
</html>
